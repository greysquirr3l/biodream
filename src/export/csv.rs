//! CSV export for biodream recordings (requires `csv` feature).
//!
//! Produces one column per channel plus a time-index column. Channel selection
//! and resampling options are handled at the [`CsvExporter`] level.

// TODO(T10): implement CsvExporter with time-index and channel selection.
